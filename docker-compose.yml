version: "3"
services:
  node_backend:
    container_name: "server"
    build: ./server/
    ports:
      - "5200:5200"
  pg:
    image: "postgres:12"
    container_name: "postgres"
    ports:
      - "5432:5432"
    volumes:
      - ./pg/db_data:/var/lib/postgresql/reviews